package animals.predators;

import animals.Predator;

public class Snake extends Predator {
    public Snake(double weight, double saturationWeight, int speed) {
        super(weight, saturationWeight, speed);
    }
}
