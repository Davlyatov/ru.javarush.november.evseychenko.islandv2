package entities.animals.predators;

import entities.animals.Predator;

public class Bear extends Predator {
    public Bear(double weight, double saturationWeight, int speed, boolean isChild) {
        super(weight, saturationWeight, speed, isChild);
    }
}
