package entities.animals.predators;

import entities.animals.Predator;

public class Eagle extends Predator {
    public Eagle(double weight, double saturationWeight, int speed) {
        super(weight, saturationWeight, speed);
    }
}
